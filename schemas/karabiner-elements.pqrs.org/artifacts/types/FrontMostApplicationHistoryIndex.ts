/**
 * A non-negative integer representing the index of the frontmost application in the history (0 = most recent, 1 = second most recent, etc.)
 */
export type FrontMostApplicationHistoryIndex = number;
